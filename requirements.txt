# Add your pinned requirements so that they can be easily installed with:
# pip install -r requirements.txt
# Remember to also add them in setup.cfg but unpinned.
# Example:
# numpy==1.13.3
# scipy==1.0

# pyspark version should correspond to the spark version.
# The current version of mleap is not compatible with spark > 2.2.0
pyspark==2.2.0
findspark
logging
uuid
